repos:
  - repo: local
    hooks:
      - id: payload-sync-check
        name: Payload snapshot sync
        entry: python3 tools/check_payload_sync.py
        language: system
        pass_filenames: false
