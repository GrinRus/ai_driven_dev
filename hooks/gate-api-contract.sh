#!/usr/bin/env bash
# Stub: API contract gate is not yet implemented in this payload.
# Exists to avoid failing hook references in settings/hooks.json.

echo "[gate-api-contract] not configured; skipping."
exit 0
