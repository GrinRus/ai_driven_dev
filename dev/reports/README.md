# dev/reports

`dev/reports/**` stores repository-maintainer artifacts used by repo tooling and CI checks.

- Source of truth for maintainer-facing reports and migration notes lives here.
- `aidd/**` is workspace data copied from `templates/aidd/**` and is not the maintainer report SoT in this repository.
