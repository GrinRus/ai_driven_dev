# ast-grep rules: custom

Place project-specific rules here.

- Add one or more `*.yaml` rule files.
- The scanner auto-includes this pack when it contains rules.
- Keep rules focused; avoid dumping large, project-unique JSON here.
