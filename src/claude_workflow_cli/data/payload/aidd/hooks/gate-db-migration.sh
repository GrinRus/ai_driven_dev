#!/usr/bin/env bash
# Stub: DB migration gate is not yet implemented in this payload.
# Exists to avoid failing hook references in settings/hooks.json.

echo "[gate-db-migration] not configured; skipping." >&2
exit 0
