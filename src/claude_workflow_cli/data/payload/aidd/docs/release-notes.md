# Release Notes

Этот файл хранит краткие заметки об изменениях workflow. Добавляйте записи в обратном хронологическом порядке.

## Формат записи
```
## vX.Y.Z — YYYY-MM-DD
### Added
- CLI-first commands: `claude-workflow prd-review`, `plan-review-gate`, `prd-review-gate`, `migrate-tasklist`, `researcher-context`, `context-gc`.

### Changed
- Hooks call `claude-workflow` directly; legacy `aidd/scripts` and `aidd/tools` are removed from the payload.

### Fixed
- ...

### Migration
- ...
```

Секции «Migration» и «Breaking changes» заполняются только при необходимости.

## vNext — YYYY-MM-DD

### Added
- ...

### Changed
- ...

### Fixed
- ...
