# Diagnostics and Logs

- Use `doctor.py` for environment/runtime path diagnostics.
- Use `tests_log.py` for structured test execution event logging.
- Use `identifiers.py` for stable ticket/slug resolution outputs.
- Keep CLI outputs compact and machine-readable where applicable.
